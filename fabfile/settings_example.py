hosts = {
	# Update this to the SSH connection string for the target TurnKey Trac vm
	'prod':'root@123.45.67.89',
}

# Some constants we'll need later
COLLATERAL_DIR = './fabfile/collateral/'
TMP_DIR = './fabfile/tmp/'

